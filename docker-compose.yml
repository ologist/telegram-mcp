services:
  telegram-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: telegram-mcp
    ports:
      - "127.0.0.1:8001:8001"
    env_file:
      - .env
    restart: unless-stopped